import 'package:flutter/material.dart';

class NewQuestionSheet extends StatelessWidget {
  const NewQuestionSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("This is the question sheet!");
  }
}
